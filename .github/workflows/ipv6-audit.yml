name: Build and audit IPv6 BGP candidates

on:
  push:
    branches:
      - ipv6
    paths-ignore:
      - 'reports/ipv6/**'
      - 'data/ipv6/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-ipv6-allowlist-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    env:
      SOURCE_DIR: ${{ github.workspace }}/.tmp/cn-eyeball-ipv6-sources
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'
          cache: true
      - name: Download IPv6 audit sources
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$SOURCE_DIR"
          curl_opts=(--fail --silent --show-error --location --retry 3)
          curl "${curl_opts[@]}" 'https://iptoasn.com/data/ip2asn-v6.tsv.gz' -o "$SOURCE_DIR/ip2asn-v6.tsv.gz"
          curl "${curl_opts[@]}" 'https://www.ris.ripe.net/dumps/riswhoisdump.IPv6.gz' -o "$SOURCE_DIR/riswhois-ipv6.gz"
          mkdir -p "$SOURCE_DIR/rdap"
          curl "${curl_opts[@]}" -H 'Accept: application/rdap+json' 'https://rdap.apnic.net/ip/240e::/18' -o "$SOURCE_DIR/rdap/chinanet.json"
          curl "${curl_opts[@]}" -H 'Accept: application/rdap+json' 'https://rdap.apnic.net/ip/2409:8000::/20' -o "$SOURCE_DIR/rdap/cmcc.json"
          curl "${curl_opts[@]}" -H 'Accept: application/rdap+json' 'https://rdap.apnic.net/ip/2408:8000::/20' -o "$SOURCE_DIR/rdap/unicom.json"
          mkdir -p "$SOURCE_DIR/atlas"
          curl "${curl_opts[@]}" 'https://atlas.ripe.net/api/v2/probes/?country_code=CN&prefix_v6=240e%3A%3A%2F18&page_size=500' -o "$SOURCE_DIR/atlas/chinanet.json"
          curl "${curl_opts[@]}" 'https://atlas.ripe.net/api/v2/probes/?country_code=CN&prefix_v6=2409%3A8000%3A%3A%2F20&page_size=500' -o "$SOURCE_DIR/atlas/cmcc.json"
          curl "${curl_opts[@]}" 'https://atlas.ripe.net/api/v2/probes/?country_code=CN&prefix_v6=2408%3A8000%3A%3A%2F20&page_size=500' -o "$SOURCE_DIR/atlas/unicom.json"
          du -h "$SOURCE_DIR"/*
      - name: Validate downloaded sources
        shell: bash
        run: |
          set -euo pipefail
          test $(stat -c '%s' "$SOURCE_DIR/ip2asn-v6.tsv.gz") -gt 100000
          test $(stat -c '%s' "$SOURCE_DIR/riswhois-ipv6.gz") -gt 1000000
          test $(stat -c '%s' "$SOURCE_DIR/rdap/chinanet.json") -gt 1000
          test $(stat -c '%s' "$SOURCE_DIR/rdap/cmcc.json") -gt 1000
          test $(stat -c '%s' "$SOURCE_DIR/rdap/unicom.json") -gt 1000
          test $(stat -c '%s' "$SOURCE_DIR/atlas/chinanet.json") -gt 1000
          test $(stat -c '%s' "$SOURCE_DIR/atlas/cmcc.json") -gt 1000
          test $(stat -c '%s' "$SOURCE_DIR/atlas/unicom.json") -gt 1000
      - name: Test IPv6 audit code
        run: go test ./...
      - name: Generate exact IPv6 BGP candidates
        shell: bash
        run: |
          go run ./cmd/build6 \
            --iptoasn "$SOURCE_DIR/ip2asn-v6.tsv.gz" \
            --ris "$SOURCE_DIR/riswhois-ipv6.gz" \
            --rdap-dir "$SOURCE_DIR/rdap" \
            --atlas-dir "$SOURCE_DIR/atlas" \
            --operator-config config/operators.json
          test -s data/ipv6/operators/chinanet.txt
          test -s data/ipv6/manifest.json
      - uses: actions/upload-artifact@v4
        with:
          name: ipv6-bgp-candidate-audit
          path: |
            reports/ipv6/
            data/ipv6/
      - name: Commit IPv6 output and audit
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A reports/ipv6 data/ipv6
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'chore: update IPv6 BGP candidates and audit'
          git push
